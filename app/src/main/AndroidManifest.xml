<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.gitai.phuckqq">

    <!--uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".PhuckQQApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <meta-data android:name="xposedmodule" android:value="true"/>
        <meta-data android:name="xposedminversion" android:value="54"/>
        <meta-data android:name="xposeddescription" android:value="@string/intercept_xposed_summary"/>
        <activity android:name=".ui.MainPreferences"
            android:label="@string/title"
            android:excludeFromRecents="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".receiver.NotificationReceiver">
            <intent-filter>
                <action android:name="me.gitai.phuckqq.QQMessage.reveiver"/>
            </intent-filter>
        </receiver>
        <service android:name=".service.QQMessageService">
            <intent-filter>
                <action android:name="me.gitai.phuckqq.service.QQMessageService" />
            </intent-filter>
        </service>
    </application>

</manifest>
